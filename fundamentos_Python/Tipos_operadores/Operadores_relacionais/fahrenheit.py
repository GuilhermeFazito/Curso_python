# Converta um número de Celsius para Fahrenheit (F = C × 1.8 + 32).

celsius = float(input("Digite a temperatura em Celsius: "))

conversor = float(celsius * 1.8 + 32)

print(f"A temperatura em fahrenheit é de: {conversor} ")